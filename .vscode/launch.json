{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch",
            "type": "node",
            "request": "launch",
            "program": "http://localhost:3000/build/Debug/${command:cmake.launchTargetFilename}",
            "preLaunchTask": "StartServer"
        }
    ]
}